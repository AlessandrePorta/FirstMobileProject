package com.example.extensivaoDoPookis

class LoggedPresenter(private val view: LoggedContract.View): LoggedContract.Presenter{

    override fun validateLogout(email:String, password: String){
        when{

        }
    }
}